# Copy to `.env.local` for local dev (`NEXT_PUBLIC_*` is embedded in the client bundle — use only for mock/demo auth).
# Replace with server-side validation + secrets in production.

NEXT_PUBLIC_MOCK_TRADER_EMAIL=trader@zayride.et
NEXT_PUBLIC_MOCK_TRADER_PASSWORD=Merkato2026!

NEXT_PUBLIC_MOCK_ALT_EMAIL=demo@merchant.et
NEXT_PUBLIC_MOCK_ALT_PASSWORD=zayride
